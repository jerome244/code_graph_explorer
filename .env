DJANGO_SECRET_KEY=change-me
DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8080
PUBLIC_ORIGIN=http://localhost:8080

# Your Postgres (running on the HOST)
DB_NAME=code_graph_explorer
DB_USER=codegraph
DB_PASSWORD=007
DB_HOST=host.docker.internal
DB_PORT=5432

# Next.js server-side calls to Django (container-to-container)
DJANGO_API_BASE=http://backend:8000
# Optional but handy for WS from the browser through Caddy:
# NEXT_PUBLIC_DJANGO_WS_BASE=ws://localhost:8080
