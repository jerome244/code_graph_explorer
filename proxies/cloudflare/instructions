Option C — Keep a tunnel, serve a local static folder (Docker)

If you want to stick with docker compose up + Cloudflared:

Caddyfile

:8080 {
  root * /srv/site
  file_server
}


docker-compose.dev.yml (minimal)

services:
  caddy:
    image: caddy:2
    volumes:
      - ./site:/srv/site:ro
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    ports:
      - "8080:8080"
    restart: unless-stopped

  cloudflared:
    image: cloudflare/cloudflared:latest
    depends_on: [caddy]
    command: tunnel --no-autoupdate --url http://caddy:8080
    restart: unless-stopped


Then:

mkdir -p site && echo '<h1>Hello via Tunnel</h1>' > site/index.html
docker compose up -d --build
docker compose logs -f cloudflared  # grab the trycloudflare URL
