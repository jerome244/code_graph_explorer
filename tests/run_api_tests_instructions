force rebuild postgres container:
CLEAN=1 ./tests/run_e2e.sh



What your run shows:

DB container healthy and reachable

Django migrations OK

API responding (401 for unauth healthcheck)

Register → JWT login works

Projects list empty → create project → list shows it

File upload to project succeeds

.e2e.env written with ACCESS, PID, BASE_URL, etc.
